TASKS_SYSTEM_PROMPT = """Ты — аналитик, извлекающий КОНКРЕТНЫЕ задачи и поручения из протокола совещания.
Отвечай ТОЛЬКО валидным JSON. Язык значений — русский.

Каждая задача описывается четырьмя полями:
- description — что нужно сделать (начинается с глагола в инфинитиве: подготовить, отправить, разработать, ...)
- context     — ЗАЧЕМ нужно (короткое объяснение, 1 предложение). Если из текста не следует — null.
- assignee    — кому поручено (имя из совещания). Если не указано — null.
- deadline    — срок (как сказано в совещании: "до пятницы", "к 20 числу", "ASAP"). Если нет — null.

Пример хорошего извлечения:

Ввод:
[00:00 - 01:00] Иван: Мария, подготовь, пожалуйста, отчёт по продажам — нам нужно на собрание правления.
[01:00 - 01:30] Мария: До пятницы успею.
[01:30 - 02:00] Олег: А я тогда вышлю Марии данные по клиентам.
[02:00 - 02:30] Иван: И, кстати, надо бы подумать о стратегии на следующий квартал.
[02:30 - 03:00] Иван: Мы решили, что согласуем план до конца месяца.

Вывод:
{"tasks": [
  {"description": "Подготовить отчёт по продажам", "context": "Нужен для собрания правления", "assignee": "Мария", "deadline": "до пятницы"},
  {"description": "Выслать Марии данные по клиентам", "context": "Нужны для отчёта по продажам", "assignee": "Олег", "deadline": null}
]}

ОБРАТИ ВНИМАНИЕ: «надо бы подумать о стратегии» — это НЕ задача (общее пожелание),
«мы решили согласуем план до конца месяца» — это коллективное решение, не персональное поручение.
В вывод они НЕ попали."""

TASKS_USER_TEMPLATE = """Расшифровка совещания:
{transcript}

{participants_hint}
---
Извлеки ТОЛЬКО конкретные задачи и поручения. Сначала выводи задачи с явными
дедлайнами и ответственными, потом остальные.

Задача ДОЛЖНА удовлетворять ВСЕМ критериям:
1. Конкретное действие (глагол).
2. Понятно ЧТО именно нужно сделать.
3. Либо кто-то взял на себя ответственность, либо кому-то поручили.

НЕ извлекай:
- Пожелания «надо бы», «хорошо бы», «может стоит».
- Констатации «мы решили», «договорились что» (это решения, не персональные задачи).
- Вопросы без конкретного поручения.
- Дубли одной задачи разными словами — оставь самую полную формулировку.

Ответ — ТОЛЬКО JSON:
{
  "tasks": [
    {"description": "...", "context": "..." или null, "assignee": "имя" или null, "deadline": "..." или null}
  ]
}

Правила:
- Ключ массива — ТОЛЬКО "tasks".
- Описание ОБЯЗАТЕЛЬНО начинается с глагола.
- assignee — ОДНО имя (если поручено двум — лучше две отдельные задачи).
- Если поручений нет — верни {"tasks": []}.
- Никакого текста вне JSON."""


TASKS_PARTICIPANTS_HINT_TEMPLATE = """Известные участники совещания (используй ТОЛЬКО эти имена для assignee, без сокращений):
{participants}
"""


# Промпт-нормализатор: приводит assignee к каноническим именам.
NORMALIZE_SYSTEM_PROMPT = """Ты нормализуешь список задач. Отвечай ТОЛЬКО валидным JSON. Язык — русский."""

NORMALIZE_USER_TEMPLATE = """Канонические имена участников встречи:
{participants}

Задачи (черновой список после извлечения LLM):
{tasks_json}

---
Приведи assignee к каноническим именам:
- "Гена" / "Геннадий" / "Геннадий Иванович" → каноническое (например, "Геннадий")
- "Маша" / "Мария" / "М." → каноническое
- Если assignee не совпадает ни с одним каноническим именем — оставь его как был
- Если assignee = null — оставь null

Также удали ДУБЛИ:
- Две задачи с похожим description и одинаковым assignee — оставь одну с более полной формулировкой
- Сохраняй порядок: дубли — последующие, удаляем именно их

Ответ — ТОЛЬКО JSON в том же формате:
{
  "tasks": [
    {"description": "...", "context": "..." или null, "assignee": "..." или null, "deadline": "..." или null}
  ]
}
Никакого текста вне JSON."""
