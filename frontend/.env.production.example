# Скопируйте в .env.production и впишите свой домен.
# .env.production читается Vite при `npm run build`.
#
# ВАЖНО: nginx на VPS проксирует /api на ваш ПК через SSH-туннель,
# поэтому VITE_API_URL = тот же домен что и фронт.

VITE_API_URL=https://meet.example.ru
